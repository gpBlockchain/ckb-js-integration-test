import {mock_rpc} from "./test_util";


describe("generate_block_with_template", function () {

    it.skip("[block_template]",async ()=>{
        let {RPCClient, requestData, responseData} = await mock_rpc()
    })
});